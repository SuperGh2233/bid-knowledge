# CHANGELOG

本文件只记录**已发布**的用户可见变更（对应 git tag）。开发过程与决策见 `docs/plans/`，
当前进度见 `docs/agent-handoff.md`，条目规范见 `docs/index.md`「新文档纪律」。

---

## v1.3 — 2026-09-17

**第二次需求对接：材料定位给「可复制正文」+ 仪器/社保检索修复 + 生成标题严格约束**

- **可复制正文段**（痛点主项）：材料定位的每条结果新增 `content_snippet` ——
  按**材料自身标题**从响应文件正文裁出**原样段落**，卡片上一键复制、CSV 单列导出。
  实测仪器清单 **636/645** 条带正文段；裁不到的如实标注「正文里没找到该类材料段落」，
  **不用文件名冒充内容**。
- **仪器定位修复**（根因）：库内 211 条「无型号无正文」的空壳行原先**排在有内容行前面**，
  用户看到的是"定位到了文件但什么都没有"。现**空壳行沉底**（SQL 判据与 Python 判据等价，零错分）。
- **纳税社保总金额**（需求1）：新增「凭证合计金额」事实类型 `finance_amount` ——
  从完税证明/社保完税凭证的**合计行**抽出（**必须有税务/社保机构痕迹**，不扫全文、不按明细求和），
  实库 **29 条 / 19 份凭证**。支持「纳税社保总金额」「社保总金额」等问法。
  ️ **社保缴费记录表没有合计行 → 抽不到就如实不产出**，不按明细求和。
- **生成标题严格约束**（需求2）：`POST /api/proposal-generate` 支持可选 `outline`
  （大标题 + 小标题清单，对象或 Markdown 文本两种形态）→ 模型**逐字**按该结构输出；
  生成后校验**如实报出漂移**（缺小标题 / 大标题不符 / 自造小节）。不传 `outline` 时行为**完全不变**。
- 修复（实施期自查）：说明性长文（`scope_note`/`mention_note`/`filter_note`/`amount_note`）里的
  `**强调**` 原先原样显示星号 → 改走项目既有的 `inline()` 渲染；`finance_amount` 行补
  `amount_note` 口径说明（非合同金额、不参与金额筛选）。
- **修复（实施期自查·两处标注分叉）**：`/api/three-modules` 原先*就地重写*了一遍角色分层，
  与 `/api/material-facts` 的 `_annotate_fact_role` 分叉 → ① **645/645** 条仪器行的
  `evidence_text` 带内部枚举前缀（`[our_response] …`）上屏；② `finance_amount` 的
  `amount_note` 整段丢失。现改为共用同一实现，实测前缀行 **645 → 0**。
- 回归：`pytest` **308 passed**（新增 10 条：锚点裁段、空壳行 SQL 等价性、金额判据正反例、
  查询映射、outline 解析/注入/校验、金额口径说明、说明文渲染护栏、两端点标注一致性）。
- 契约：`docs/specs/api.md` §4 / §4A / §5 已同步。

## v1.2 — 2026-09-17

**「正文提及」合同组：明细里没有、合同正文写着的产品也能查到（零外发）**

- **问题**：5 份合同明细里没有目标产品、但**正文写着**（如「多组学检测」合同正文含
  「空间代谢组学分析」）→ 检索一条不产出，金标准 Recall 复测 28/37。
- **修法（用户裁定，方案 B）**：检索层新增「正文提及」组 —— 产品词出现在**业务上下文**
  （±12 字内）才算提及；**纯本地规则、零外发**（LLM 语义复核授权标「暂缓启用」）。
- **四条硬约束**：① **不参与金额筛选**（金额只作合同总额展示并标注）；② **永不进 `hits`**
  （独立分组、琥珀色标记）；③ 排除条件按合同级作用于正文（「不要蛋白组」实测误返 10→1）；
  ④ 每条带**正文依据原文**上屏 + 可打开源文件。
- **结果**：金标准 Recall **37/37 = 100%**、误返 **0**（表面 1 条系金标准自相矛盾，
  已裁定为标注瑕疵并留痕）；`pytest` **298 passed**。
- **口径更正**：此前报告的「92.3%→75.7% 退化」经复核**证伪** —— 实为分母从 26 扩到 37
  （可检索合同 98→136 份），明细路径对老分母的召回两轮一致（24/26）。
- 契约：`docs/specs/api.md` §2B（`mention_contracts` / `mention_note`）；
  决策全文：`docs/plans/active/PLAN-20260917-contract-product-mention.md`。
- 补建 `.env.example`（README/AGENTS.md 一直引用但仓库缺失）。

## v1.1.2 — 2026-09-17

**业绩清单：金额覆盖 40% → 95%，且合同金额开始参与金额条件**

- **业绩清单（响应文件内的历史合同声明）** `LEDGER-*`：**29 → 408 条 / 4 → 51 份**；
  **有金额 50 → 388 条（95%）**，逐条反查证据原文 **0 条对不上**。
- **金额口径改判**（用户要求）：业绩行的**合同总额开始参与金额筛选** ——
  改前先实测（127 份合同按 1万/5万/10万/30万/50万 门槛比对总额与产品金额），**误返 0 份、漏召最多 2 份**。
  未达门槛只报数、**金额未记载的单列给出文件**（不静默丢结果）。
- **抽取覆盖面**（按根本原因改判据，非逐个补案例）：结构判据认项目列 · 按列名定位 ·
  合并列取格尾数字 · 行缓冲遇新表头即收尾 · 可疑解析**允许 upsert 禁止删除** ·
  表头判据放宽 · 同文档合同清单**交叉引用补数**（三种金额写法 + 唯一性守卫）。
- **误抽拦截**：偏离表冒充业绩表、关联方表把电话号码当金额 → 定向清库 130 条假行。
- **计数口径修正**：`/api/status` 的合同数**只数 `CTL-*`**（原先整表计数把业绩行也算进"合同"）。
- **展示**：业绩行与合同原件**同一列表** + 小标记；证据限长 300 字 / 卡片 160 字；
  未入选去向可见（未达门槛计数 + 金额未记载折叠列表）。
- **无金额的行如实标注原因**：`表未设金额列` / `本行未取到`（不再含混显示"未记载"）。
- 回归：`pytest` **298 passed**（含用户报告案例的形态固化测试）。

## v1.1.1 — 2026-09-16

**业绩清单进入检索**

- `LEDGER-*` 从 29 条补到 **408 条 / 51 份**（首轮 126 条 / 16 份），有金额 50 → 388 条。
- 业绩行进入需求一检索：**与合同原件同一列表**、带 `业绩声明` 小标记、可打开源文件；
  同一份声明在多个项目文件夹各存一份时**只展示一次**（副本位置留在 `also_in`）。
- **独立闸**：业绩行不进 `approved_documents.json`、不计入 `queryable_contracts`、**永不进 `hits`**。
- 新增脚本 `scripts/backfill_track_records.py`（写前自动备份、拒写正式库）。

## v1.1 — 2026-09-16

**需求方试用反馈整改收口 + 查询意图识别 + 文档体系化**

- 需求方 5 条反馈全部实施：覆盖面公示 · 合同补齐（127 → 165，白名单 95 → 136）·
  平台别名（`Visium HD`/`Stereo-seq` 并入空间转录组，`Xenium` 单列）· 社保月份六轮修复 ·
  仪器通称→型号（`质谱仪`/`生物分析仪`）· 模糊检索（`华大转录组30万以上` 不再被拦）· 方案产品线维度。
- **查询意图识别层**（`app/intent.py`）：本地优先、判不出才问模型（只外发用户那一句查询）。
- 三类材料卡片补齐「打开文件/所在文件夹/复制路径」。
- 文档体系化：`AGENTS.md` 落仓、`docs/` 按类型重组（specs/plans/authorizations/evals/business）、
  外发授权集中登记；旧系统移出工作区归档。

## v1.0.0 — 2026-09-16

**首个可交付版本（最小重建版）**

- 需求一 历史材料定位：合同定位（产品/金额/日期/机构/排除）、材料存在性、三类材料、方案章节；
  合同定位覆盖 **136 份已核合同**。
- 需求二 模块级方案生成：8 个方案模块，受证据约束 + 带出处（引用编号由系统生成）。
- 退出门槛达标：Recall 92.3% / 真实路径 100% / 金额条件 100% / 误返 0 / 必要小节覆盖 8/8。