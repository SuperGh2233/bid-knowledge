# OCR 外发授权记录（2026-09-10）

> 本文件在**执行任何 OCR 调用之前**写入，用于补齐此前"授权未落成书面语句"的缺口。

## 一、授权语句

用户在 2026-09-10 选择方案 **A「授权 OCR」**，用于解除合同定位能力的扩展阻塞。

**授权范围为下列范围，不含其他：**

> 允许将 `bid_ai_clean_reg.db` 中 `document_role='contract_evidence'` 的文件内容
> （157 份扫描 PDF 的逐页图片、1174 张 jpg/png 扫描图），发送至 `.env` 配置的
> OCR 网关（`https://newapi.oebiotech.com/v1`，模型 `qwen3.7-flash`），
> 用于提取合同正文以支持产品明细金额定位。
>
> **兜底网关 `https://mineru.net` 为公网服务**，仅在 qwen 通道失败时使用。
>
> 本轮**先做 5 份小样（约 90 页）**，验证质量后再决定是否批量。

## 二、外发规模

| 项 | 数量 |
|---|---|
| `contract_evidence` 合计 | 1331 份 |
| 扫描 PDF（逐页转图） | 157 份 / **1724 页** |
| 扫描图片 jpg/png | 1174 张 |
| **合计待识别** | **约 2898 页/图** |
| **本轮小样** | **5 份 PDF，约 90 页** |

## 三、外发内容性质（必须知情）

这些是**已签署的合同原件扫描件**，含：

- 甲方/乙方**单位全称**
- **合同金额**（成交价）
- 项目负责人**姓名、联系方式**
- 签署日期、盖章
- 服务明细与单价

**与之前外发的投标响应文件相比，合同原件的商业敏感度更高**（含成交价与对方单位）。
本记录如实标注这一点。

## 四、网关

| 用途 | 目标 | 是否公网 |
|---|---|---|
| 主通道 | `https://newapi.oebiotech.com/v1`（qwen3.7-flash） | 公司网关 |
| 兜底 | `https://mineru.net` | **公网第三方** |

## 五、本轮不做

- 不 OCR 非 `contract_evidence` 的文件
- 不做全量 2898 页（先小样）
- 不写正式库 `bid_ai_clean.db`
- 不重建 ES 向量索引

## 五之二、敏感信息处置口径（2026-09-10 用户裁定）

合同 OCR 正文含**双方当事人的姓名、个人手机号、个人邮箱、成交价**。用户裁定：

> **原样存。不做脱敏。**

**记录在案**：这意味着解析产物与后续检索索引中会保留上述个人信息。
后续如需对外分享检索结果、或把数据迁到正式库/ES 索引，**须重新确认该口径**。
本决定**不随本轮结束而自动延续到对外场景**。

## 六、执行前状态冻结

- 规则：`app/extract.py` SHA `4bed1412025b459662e95f5679bd4c6075e8a572b69515c340cb343a58d084e4`（未变）
- 测试：`pytest` 104 passed
- 测试库备份：`bid_ai_clean_reg.bak-*`
