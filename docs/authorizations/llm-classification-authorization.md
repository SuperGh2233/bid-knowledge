# LLM 语义分类授权记录（2026-09-11）

> 本文件在**执行任何 LLM 调用之前**写入。

## 一、授权语句

用户在 2026-09-11 授权执行材料事实提取的**第二段：候选行语义分类**。

**授权范围：**

> 允许将 `bid_ai_clean_reg.db` 中 `document_role IN ('our_response','final_signed')` 的
> 响应文件正文里，**经确定性规则筛出的候选行**（短行、含材料语义词），
> 发送至 `.env` 配置的 LLM 网关（`https://newapi.oebiotech.com/v1`），
> 用于判断该行属于：**我方已附材料 / 采购人要求 / 承诺函正文 / 章节标题**。

## 二、外发规模（与已授权的 OCR 对比）

| | OCR（已执行） | 本次 LLM |
|---|---|---|
| 外发单元 | 1724 页扫描图 + 1174 张图 | **候选行文本** |
| 规模 | 2898 页/图 | 预计 **数百行短文本** |
| 性质 | 合同原件图像（含甲乙双方、成交价、经办人） | 响应文件里的清单行文本 |

**本次外发量比 OCR 小几个数量级。**

## 三、为什么需要它（对抗复核的结论）

全清单解析的**确定性判据本质是语义的**，4 路对抗复核一致判定"不能用"：

```
（五）拟派项目实施团队（含社保证明）    ← 章节标题，不该抓
（三）财务状况表或银行资信证明          ← 材料条目，该抓
```
**语法完全同构，词表划不开。**

复核给出的路径：**结构特征找候选（确定性） → 语义分类 source（LLM） → 字段抽取（确定性）**。

## 四、外发内容脱敏约束

- **只发候选行本身**，不整篇发送；
- 候选行若含身份证号/手机号等，**发送前打码**；
- 不发文件路径、不发文档名。

## 五、本轮不做

- 不做全清单逐行外发（只发候选行）
- 不写正式库 `bid_ai_clean.db`
- 不重建 ES 索引
