# 授权记录：我方响应/最终版扫描件 OCR（2026-09-11 扩大范围）

> **本记录是对 `docs/ocr-authorization.md` 范围的扩大**，不替代它。原记录只覆盖
> `contract_evidence` 的扫描件（157 份 / 1,435 页，已完成）。

## 1. 授权事项

对我方响应文件（`our_response` / `final_signed`）中的**扫描 PDF 与图片**做 OCR，
文字结果写回本地测试库（派生物只写本地，NAS 只读）。

## 2. 授权依据

- **2026-09-11 用户明确指示**：就「640 份必须重做 OCR（外发）」回复「**允许**」。
- 该指示由用户本人作出，且**点名了动作（重做 OCR）与外发性质**，构成对本次扩大范围的有效授权。

## 3. 范围与网关

- **主体**：`document_role ∈ {our_response, final_signed}` 且尚无解析产物的 `.pdf/.jpg/.png`。
- **网关**：`.env` 的 `LLM_BASE_URL`（公司自有网关 `newapi.oebiotech.com`）。
- **兜底 MinerU（公网第三方）保持关闭**（`OCR_FALLBACK_MINERU=false`）—— 本批**不使用任何公网兜底**。
- **不含**：竞品响应、招标要求、未知角色、空模板；`.doc/.xls` 等非图像格式。

## 4. 实测体量（发起前已量清）

| | |
|---|---|
| 候选文件 | **598 份** |
| 抽样 40 份：纯扫描占比 | **34/40** |
| 外推需 OCR | **约 508 份 ≈ 72,500 页** |
| 对比既有批次 | 157 份 / 1,435 页 → **51×** |
| 字节总量 | 5.80 GB |

**按大小分布分批**（一半以上文件不到 2 MB，却只占 1% 字节）：

| 阶段 | 大小 | 文件数 | 占字节 |
|---|---|---|---|
| **1** | < 5 MB | **346（58%）** | **3%** |
| 2 | 5–15 MB | 108 | 18% |
| 3 | 15–40 MB | 122 | 48% |
| 4 | ≥ 40 MB | 22 | 30% |

## 5. 执行方式

- 复用 `scripts/ocr_batch.py`（**可断点续跑 + 按内容 SHA 幂等**），加 `--roles` / `--max-mb` 参数；状态文件按角色分开。
- **单文件失败不阻塞**，如实记录；不伪装成功。
- **分阶段执行**：先跑阶段 1（346 份、仅 3% 字节），据实测吞吐再决定后续阶段节奏。

## 6. 未决 / 风险

- 全量约 **60–121 小时**（按单页 3–6 秒估算），须分批推进、可随时中断续跑。
- 超大文件（≥40 MB、22 份、30% 字节）建议**单独评估**——其中含 605 页级文档。
- 本记录**不覆盖**：竞品/招标文件、公网 OCR 兜底、以及把正文送 LLM 生成（那是
  `docs/llm-generation-authorization.md` 的范围）。
